// This file was generated by Ansible for {{ansible_fqdn}}
// Do NOT modify this file by hand!

// Graphite-beacon configuration file
{

    // Graphite server URL
    "graphite_url": "{{graphite_beacon_graphite_url}}",

    // HTTP AUTH username
    "auth_username": {{graphite_beacon_auth_username|to_nice_json}},

    // HTTP AUTH password
    "auth_password": {{graphite_beacon_auth_password|to_nice_json}},

    // Path to a pidfile
    "pidfile": {{graphite_beacon_pidfile|to_nice_json}},

    // Default values format (none, bytes, s, ms, short)
    // Can be redfined for each alert.
    "format": "short",

    // Default query interval
    // Can be redfined for each alert.
    "interval": "10minute",

    // Notification repeat interval
    // If an alert is failed, its notification will be repeated with the interval below
    "repeat_interval": "{{graphite_beacon_repeat}}",

    // Default loglevel
    "logging": {{graphite_beacon_logging|to_nice_json}},

    // Default method (average, last_value).
    "method": {{graphite_beacon_method|to_nice_json}},

    // Default prefix (used for notifications)
    "prefix": {{graphite_beacon_prefix|to_nice_json}},

    "send_initial": {{graphite_beacon_send_initial|to_nice_json}},

    // Default handlers (log, smtp, hipchat)
    "critical_handlers": {{graphite_beacon_critical_handlers|to_nice_json}},
    "warning_handlers": {{graphite_beacon_warning_handlers|to_nice_json}},
    "normal_handlers": {{graphite_beacon_normal_handlers|to_nice_json}},

    // SMTP settings
    "smtp": {
        "from": {{graphite_beacon_smtp_from|to_nice_json}},
        "to": {{graphite_beacon_smtp_to|to_nice_json}},
        "host": {{graphite_beacon_smtp_host|to_nice_json}},
        "port": {{graphite_beacon_smtp_port|to_nice_json}},
        "username": {{graphite_beacon_smtp_username|to_nice_json}},
        "password": {{graphite_beacon_smtp_password|to_nice_json}},
        "use_tls": {{graphite_beacon_smtp_use_tls|to_nice_json}}
    },

    // Hipchat settings
    "hipchat": {
        "room": {{graphite_beacon_hipchat_room|to_nice_json}},
        "key": {{graphite_beacon_hipchat_key|to_nice_json}}
    },

    // Place your alerts here
    "alerts": {{graphite_beacon_alerts|to_nice_json}}

}
